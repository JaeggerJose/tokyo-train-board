"""Bundled csl status-line themes (package data).

This subpackage is the *canonical* home of the portable csl themes; the
repo-level ``integrations/csl/`` copy is kept byte-identical by a drift-guard
test. Shipping them as package data lets a pip-installed user run
``jrboard install-csl-theme`` with no clone and no manual ``cp``.
"""
